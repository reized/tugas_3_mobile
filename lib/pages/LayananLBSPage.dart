import 'package:flutter/material.dart';

class LayananLBSPage extends StatelessWidget {
  const LayananLBSPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("success"),
    );
  }
}
